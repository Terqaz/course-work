<form method="post" action="{{ path('app_branch_message_delete', {'id': branch_message.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ branch_message.id) }}">
    <button class="btn">Delete</button>
</form>
