{% extends 'base.html.twig' %}

{% block title %}DialogMessage{% endblock %}

{% block body %}
    <h1>DialogMessage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dialog_message.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dialog_message_index') }}">back to list</a>

    <a href="{{ path('app_dialog_message_edit', {'id': dialog_message.id}) }}">edit</a>

    {{ include('dialog_message/_delete_form.html.twig') }}
{% endblock %}
